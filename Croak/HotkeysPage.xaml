<Page
    x:Class="Croak.HotkeysPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:c="using:Croak"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer>
        <Grid Grid.Row="1" RowSpacing="15" Padding="15,15,15,15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <ListView x:Name="HotKeysListView" Grid.Row="0" SelectionMode="None" Loading="HotKeysListView_Loading">
                <ListView.Resources>
                    <Style TargetType="ListViewItem" BasedOn="{StaticResource ListViewItemSettingStyle}"/>
                </ListView.Resources>
            </ListView>

            <c:HotKeysViewer x:Name="HotKeysViewer" Grid.Row="1" ShowMouseMap="False"/>

            <c:MessageBar x:Name="UserMessageBar" Visibility="Collapsed" VerticalAlignment="Top" Margin="10,4" Grid.RowSpan="3"/>
            
        </Grid>
    </ScrollViewer>
</Page>
